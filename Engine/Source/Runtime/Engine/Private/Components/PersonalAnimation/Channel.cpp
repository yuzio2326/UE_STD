#include "Channel.h"

UChannel::UChannel()
{


}

UChannel* UChannel::Create(FNodeAnim* pAIChannel)
{
	//UChannel* pInstance = new UChannel();

	//UChannel의 Initialize를 언제 호출할지가 좀 고민인데 ... HRESULT를 사용해서 할까?
	//if (FAILED(pInstance->Initialize(pAIChannel)))	
	//{
	//	_ASSERT(TEXT("Failed To Created : CChannel"));
	//	Safe_Release(pInstance);
	//}

	//return pInstance;
}